# 修正項目一覧（2026-07-06）

> `プロジェクトレビュー_20260706.md` の指摘を、テスト再開前に対応する項目として優先度順に整理。
> Haiku移行専用の項目（ゴールドセット・回帰ハーネス・確信度出力等）とbacklog項目は対象外（レビュー本文の「Haiku移行時にやる」「backlogのままでよい」を参照）。

## 最優先（データの正しさに直結・High severity）

- [x] **#1 正規化をコード側へ移設**（完了 2026-07-06）
  `append_sheet.py`に検証・正規化層を新設。開始時期→`YYYY-MM`（年欠落は`received_at`年で補完、複数月併記は最早月）、勤務地_県は駅/市区→県の対応表で機械補完（未収録は空欄+`[warn]`表示、フルリモート等は警告対象外）、リモートは`{フルリモート/一部リモート/常駐}`のenumに写像し限定条件は括弧で保持。Claudeは原文抽出のみ担当するよう`docs/skills/ses-structure.md`・`.claude/skills/ses-structure/SKILL.md`を更新。既存の実データ44件でドライラン検証済み（警告0件）。
  出所: レビュー #1・#5

- [x] **#2 全21キー列挙＋キー脱落防止**（完了 2026-07-06）
  `docs/skills/ses-structure.md`・`.claude/skills/ses-structure/SKILL.md`の出力契約に全21キーの完全な1行例3パターン（通常/フルリモートで勤務地_県が正当に空欄/複数案件分解）を追加し「欠けたキーは空文字で必ず出力」を明文化。`append_sheet.py`に`PARSED_KEYS`を追加し、欠落キー・想定外キーを`_build_row`でwarn。既存データ・合成データで動作確認済み。
  出所: レビュー #6

- [x] **#3 規模前提を実測値に改訂**（完了 2026-07-06）
  CLAUDE.md（§規模の前提・§アーキテクチャ図・§v0実装契約のバッチ記述）とDECISIONS.md §3を「1日約1,000通」→実測5,455通/日（案件約1,850通/日）へ更新し、無人化前倒しの必要性を明記。TODO.mdの2026-07-01完了ログは履歴のため据え置き（append-only方針）。
  出所: レビュー #3前半

- [x] **#4 fetch段の定型除去**（完了 2026-07-06）
  `fetch_gmail.py`に`_clean_body`（`\r\n`正規化→フッター切除→URL短縮→装飾セパレータ/空行圧縮）を追加し`_extract_body`の出口で適用。既存inbox実データ400件でドライラン検証: 平均14.4%削減・過剰カット（空文字化/大幅欠落）0件。追跡URL塊のニュースレター（18,233字）は3,893字まで削減。可逆性は`案件メールリンク`（messageIdからGmail原文へ復元）で担保。
  出所: レビュー #4

## 優先（運用の穴・Medium severity）

- [x] **#5 実行コマンドのOS非依存化**（完了 2026-07-06）
  3つのSKILL.md＋対応する仕様書（docs/skills/*.md）の`.venv\Scripts\python.exe`等をmacOS/Linux併記に修正。triageの手動カウント（`Get-Content|Measure-Object`）は`scripts/verify_triage.py`（新規・OS非依存）へ置き換え。#10と合わせて対応。
  出所: レビュー #9

- [x] **#6 append_sheetの取りこぼし防止手順明記**（完了 2026-07-06）
  `docs/skills/ses-structure.md`・SKILL.md の手順4に「append_sheetは`data/inbox`/`data/parsed`を全globする＝対象は今回分でなく現存全ファイル」を明示し、「バッチ完了ごとに処理済みを`_archive`へ退避してからappend」を強調。手順7を「実質必須」に格上げ、SKILL.md冒頭の不変条件にもglob警告を追加。将来のバッチ指定引数はbacklog明記。
  出所: レビュー #10

- [x] **#7 triage判断基準の明文化**（完了 2026-07-06）
  正本`docs/skills/ses-triage.md`とSKILL.mdの判断基準に「決め手（優先順位順）」節を追加：①依頼文の向き（`見合う人材を探しております`＝案件／`ご提案可能な案件`＝人材）②単価トークンの向き（`MAX◯`＝案件／`@◯万〜`＝人材）③主語（弊社プロパー/イニシャル＋年齢＝人材）。「件名だけで決めない」を明記し、同じ「ご紹介」件名でも本文で割れる対照few-shot3件（実データ`19f34b0be02ec20f`等に基づく）を追加。
  出所: レビュー 追補①②

- [x] **#8 その他偽装への対処**（完了 2026-07-06）
  正本ses-triage.md・SKILL.mdのその他行に「件名にマーカーがあっても本文に実体が無ければその他」を明記。`fetch_gmail.py`に`SKIP_BODY_DOMAINS`（送信元ドメイン軽量スキップリスト・`ligare.news`をシード）を追加し、該当配信元の本文をマーカーへ差し替え（メールは残しラベル付与＝取りこぼしなし・末尾一致の誤爆なしをテスト確認）。CLAUDE.md/DECISIONS.mdの「ノイズ無し」記述を「ノイズ極少（ゼロではない）」に緩和。
  出所: レビュー #7

- [x] **#9 dedup記述の統一**（完了 2026-07-06）
  dedup（重複回避）の記述をCLAUDE.md・`append_sheet.py` docstringともに実装通り「row_key完全一致」へ修正（`messageId#1`/`#2`が別行として両方残る＝複数案件を取りこぼさない旨も明記）。**区別が重要**: `/ses-structure`の突合確認（triage案件→parsedの照合）は`messageId#連番`を拾う「前方一致」が正しいため、そちらは変更せず維持。CLAUDE.mdに両者の違いを追記。
  出所: レビュー skill-structure(low)

- [x] **#10 triage突合検証の正本反映**（完了 2026-07-06。#5で実装した`verify_triage.py`と併せて対応）
  正本`docs/skills/ses-triage.md`に（従来SKILL.mdにしか無かった）突合検証ステップを正式に追加し、`scripts/verify_triage.py`にmessageId集合の完全一致に加え`分類`3値チェック・`理由`非空チェックを実装。実データ2件（50件/320件）で誤検知0件を確認。
  出所: レビュー一覧内

## 軽微

- [x] **#11 本文空/文字化け時の件名判定緩和**（完了 2026-07-06）
  正本ses-triage.md・SKILL.mdの倒し方を「本文が技術的に読めない（空/文字化け/スタブ/HTML抽出失敗）場合はまず件名を見て、明確な方向シグナル（【当社社員】＝人材／必須スキル＋単価上限＝案件）があれば件名で判定・理由に確信度低を明記」へ緩和。#8（本文が読めるが実体なし→その他）との線引きを両ファイルに明示。
  出所: レビュー 追補④

## 今回は対応しない（後回し・参考）

- **Haiku移行時にやる**: ゴールドセット＋回帰ハーネス構築／確信度出力・人間確認キュー／Haiku API実測／無人化アーキ／triage↔structure二重読み込み解消
- **backlogのまま**: 転送・引用ネスト保持／HTML表TSV化／近重複デデュープ等
