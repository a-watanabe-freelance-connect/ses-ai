# 仕様書: /ses-match — マッチングスキル（要員→案件）

- 作成日: 2026-07-03 ／ 対象: v0（方向Aのみ）
- 根拠: `CLAUDE.md` §実現2、`DECISIONS.md` §4（v0の作り）・§8（3スキル構成）
- 実装先: `.claude/skills/ses-match/SKILL.md`（**本仕様書が正本**。既存 SKILL.md（2026-07-03作成）は本仕様と整合済み・以後の変更は本仕様を先に更新する）

## 1. 目的・位置づけ

`案件台帳` を**全件読み**、指定要員のプロフィール（`要員/<名前>.md`）＋その場の**許容条件プロンプト**と照合してマッチング評価し、ランキングを画面表示・本人タブへ1行1案件で蓄積する。v0 パイプラインの第3スキル（実現2・方向A）。

```
案件台帳(全件) + 要員/<名前>.md + 許容条件プロンプト → 【/ses-match 本スキル】
   → 画面表示（ランキング） + data/matches/<名前>.jsonl → write_match.py → 本人タブ
```

- しきい値表による機械フィルタではなく、許容条件（隣県可・経験±等）込みで **Claude が都度判断**する。
- 方向B（案件→人材）は将来スコープ。

## 2. 起動

- 起動例: 「この要員に合う案件を出して。経験3年だが優秀なので4年要件まで可、東京＋隣県で」「渡辺さんのマッチングをして」
- 許容条件プロンプトは本人条件（md）への**上書き・追加**として都度適用する。

## 3. 前提条件

- `要員/<名前>.md` が用意されていること（未整備なら本人へ確認、またはスキルシート等の既存資料からテンプレート `要員/_テンプレート.md` を埋める）。
- `.env` に `SHEET_ID` 設定済み、OAuth 設定済み、`.venv` 構築済み。

## 4. 入力（データ契約）

- **案件**: `scripts/read_sheet.py`（Windows: `.venv\Scripts\python.exe scripts\read_sheet.py` / macOS・Linux: `.venv/bin/python scripts/read_sheet.py`） → `案件台帳` 全件を、ヘッダー行をキーにした dict のリスト（JSON）で得る（列構成の変更に自動追従）。
  - 注意: シェルの `>` リダイレクトは cp932 文字化けの恐れ。ツールの出力キャプチャで直接読むか、ファイル保存が必要なら UTF-8 でのファイル書き込みで行う。
  - 件数が多い場合の分担は件数次第で判断してよいが、マッチング判断は業務理解が要るため基本はメインの会話で評価する（精度優先）。
- **要員**: `要員/<名前>.md`（箇条書き: スキル/経験年数/希望勤務地/希望単価/リモート希望/稼働開始 等）＋許容条件プロンプト。

## 5. 評価（判断基準）

**評価軸**（★=案件/人材で対称。ただし単価と経験は非対称）:

| 軸 | 判定 |
|---|---|
| 必須スキル充足 | プロフィールの経歴・スキルとの照合 |
| 経験年数 | 許容条件込み（「3年だが4年要件まで可」等） |
| 勤務地 | 居住地・希望県＋隣県許容込み |
| 単価 | **案件=上限予算 vs 人材=下限希望**。区間の重なりで判定 |
| リモート | 希望形態との整合 |
| 開始時期 | 稼働開始との整合 |
| 商流 | 深い商流の懸念等 |

**判定ルール**:
- 適合度は3段階: **◎**（強い一致）／**○**（好条件だが一部懸念）／**△**（部分一致・要確認点が多い）。
- **国籍・年齢・性別は機械足切りにしない**（職業安定法・労働施策総合推進法9条）。案件側に制限記載があっても参考情報として懸念欄に記すに留め、適法性要確認の警告を添える。
- 明確に不一致（言語・職種・経験年数が大きく乖離等）は対象外としてよい。ただし「惜しい」候補（スキルは合うが勤務地がネック等）は積極的に△で残す（機械的除外より人間の判断に委ねる）。
- 適合理由・懸念はプロフィールの具体的根拠と紐付けて記述する（例: 「PJ4の出荷管理システム経験がこの物流案件のドメインと一致」）。
- **適用した許容条件は適合理由に明記**する（再現性のため。結果行に残る）。

## 6. 処理手順

1. `read_sheet.py` で案件台帳を全件読む。
2. `要員/<名前>.md` を読み、許容条件プロンプトがあれば上書き・追加適用。
3. §5 の基準で全案件を評価しランキング。
4. `data/matches/<名前>.jsonl` へ1行1案件で書き出す（既存ファイルがあれば末尾追記。write_match 側で row_key dedup されるため再掲しても二重行にはならない）。
5. `scripts/write_match.py <要員名>` で本人タブへ反映（Windows: `.venv\Scripts\python.exe scripts\write_match.py <要員名>` / macOS・Linux: `.venv/bin/python scripts/write_match.py <要員名>`）。
6. §8 のルールで画面報告。

## 7. 出力（データ契約）

### data/matches/<名前>.jsonl（1行1案件）

```json
{"row_key": "案件台帳のrow_key", "適合度": "◎", "案件名": "...", "単価": "...", "勤務地": "...",
 "リモート": "...", "商流": "...", "必須スキル": "...", "適合理由": "...（許容条件の適用を明記）",
 "懸念": "...", "リンク": "案件メールリンク"}
```

### 本人タブ（タブ名＝要員名。write_match.py が書く）

- 列: `記載日 / 適合度 / 案件名 / 単価 / 勤務地 / リモート / 商流 / 必須スキル / 適合理由 / 懸念 / ステータス / 案件row_key(messageId) / リンク`
- `記載日`＝実行日・`ステータス`＝未指定なら「未対応」をスクリプトが自動設定。
- タブが無ければ見出し行付きで自動作成。既存タブに見出しが無ければ補完（`_ensure_tab`）。実行後、タブ1行目が見出しになっているか確認する。
- **既存の `案件row_key(messageId)` 列で重複回避**（再実行は新規のみ・既存行の手動ステータスは保持）。

## 8. 完了報告（画面表示ルール・2026-07-03決定）

冒頭で必ず ◎/○/△ の件数サマリーを示す。詳細ブロックの表示範囲は **◎の件数**で切り替える（コンテキスト消費とのバランス）:

- **◎が10件以内**: ◎と○を1件1ブロックでフル表示。△は件数のみ＋「詳細は本人タブでご確認ください」。
- **◎が10件超**: ◎のみ1件1ブロックでフル表示。○・△は件数のみ。

ブロックのフォーマット:

```
### <適合度> <案件名>
- 単価: XX万円 / 勤務地: XXX / リモート: XXX / 商流: XXX
- 必須スキル: XXX
- 適合理由: XXX（許容条件を適用した場合は明記）
- 懸念・要確認: XXX
- リンク: <案件メールリンク>
```

## 9. エラー・例外

- `要員/<名前>.md` が無い → マッチングを実行せず、本人確認またはテンプレートからの作成を案内。
- マッチ結果ファイル無しで write_match.py 実行 → FileNotFoundError（先に評価・書き出しを行う）。
- 案件台帳が空 → 「先に `/ses-triage` → `/ses-structure` で取込を」と案内。

## 10. スコープ外（backlog / 将来）

- 方向B（案件→人材・`マッチ結果` タブ）→ 将来。
- `person_id` 主キー・行ロック（TOCTOU回避）→ backlog（v0 はタブ名＝要員名）。
- 鮮度TTL（古い案件の減点・除外）・粗フィルタ緩和（隣接県表）・スキル正準辞書 → backlog（v0 は全件を Claude が読む）。
- 単一マッチ結果タブへの移行（人員タブ>30）→ backlog。
